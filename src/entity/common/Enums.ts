export enum ArticleCategory {
  study = 'study',
  project = 'project',
}

export enum ArticleProceed {
  online = 'online',
  offline = 'offline',
}

export enum ArticleContact {
  kakao = 'kakao',
  email = 'email',
  google = 'google',
}
